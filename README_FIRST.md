# README FIRST — YUNY CURRENT

This repo is the GitHub support tree for the YUNY Suno production runtime. The install source of truth is the package root:
- Project Instructions → `1_PROJECT_INSTRUCTIONS/PROJECT_INSTRUCTIONS.txt`
- Knowledge (20) → `2_KNOWLEDGE_FILES/`
- GitHub → full contents of `3_GITHUB_UPLOAD_STRUCTURE/yuny-suno-os-main/` uploaded to repo root

See `CURRENT_FULL_PACKAGE_APPLY_GUIDE.md` and `INSTALL_SOURCE_CURRENT.md` for exact apply steps.

## Private overlay (optional)
`vault/operator-private/` holds a taste-memory layer distilled from the operator catalog. It is GitHub/reference only — never upload it to GPT Knowledge. Use on invocation only. No raw lyrics are stored in the generated profile files.

## Legacy / reference
`archive/` and any historically named file are reference/craft material, not install source. Preserved on purpose; the runtime never routes install through them.
