# INSTALL — OPERATOR CANON (CURRENT)

이 패키지가 설치 소스의 단일 진실이다. 구버전 zip 델타가 아니라 **현재 풀 패키지**를 설치한다.

## 설치 3단계
1. Project Instructions 교체 → `1_PROJECT_INSTRUCTIONS/PROJECT_INSTRUCTIONS.txt` (8,000자 미만).
2. Knowledge 20장 교체 → `2_KNOWLEDGE_FILES/` 전체(파일명 동일 드롭인, 20장 유지).
3. GitHub: `3_GITHUB_UPLOAD_STRUCTURE/yuny-suno-os-main/` **전체**를 repo 루트에 업로드(패치만 X).

## operator-history 취급
`operator-history/`·`vault/operator-private/`는 **GitHub/참조 전용, GPT Knowledge 업로드 금지**. 호출 시에만 사용.

## Smoke 3종 (새 창)
S1 "내 결로 여름곡 하나, 네르로" → operator-history fetch 흔적 + 네르 기존 결 적극 반영(회피 아님) + 캐릭터명이 프롬프트 필드에 없음([VOICE] 물리만).
S2 "아티스트 OO-style로" → 분해 기본 적용 또는 1회 시도+분해 폴백 동봉, 비율 표기는 순서 변환.
S3 듀엣 의뢰 → 리스크 1줄 고지 + [V1 female]/[V2 male] 강화책 적용.

## 상태
준비 전용. 라이브 Project는 수동 적용 후에만 갱신. GitHub는 전체 트리 업로드 후에만 current.
