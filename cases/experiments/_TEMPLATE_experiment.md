# C-YYYYMMDD-NN — [experiment slug]

> Experiment = deliberate test of an unproven technique. Uses case.schema.md PLUS the hypothesis block below. Never cite an experiment as if it were a promoted pattern.

```yaml
hypothesis: # what we expect and WHY (mechanism)
variable: # the ONE thing changed vs control
control_ref: # baseline generation/case being compared against
success_criteria: # decided BEFORE generating
```

[then full case.schema.md block]

Outcome routing: success ×3 → prompt-pattern promotion · failure → keep (negative results are knowledge) · inconclusive → refine variable, re-run.
