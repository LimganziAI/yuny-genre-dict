# CURRENT Install Guide

Use only the current install mirror:

```text
project-sync/PROJECT_INSTRUCTIONS.txt
project-sync/knowledge-20/
```

Install steps:
1. Copy `project-sync/PROJECT_INSTRUCTIONS.txt` into Project Instructions.
2. Upload exactly the 20 markdown files from `project-sync/knowledge-20/` into Knowledge.
3. Keep the rest of this repository as GitHub lookup material.

Do not use older install docs or archived package names as the install source.
