# CURRENT FULL PACKAGE — APPLY GUIDE

This is the full install package (not a patch). Apply all three install folders.

## 1. Project Instructions
Replace the live Project Instructions with:
`1_PROJECT_INSTRUCTIONS/PROJECT_INSTRUCTIONS.txt`

## 2. Knowledge (exactly 20)
Replace all GPT Knowledge files with the 20 files in:
`2_KNOWLEDGE_FILES/`

## 3. GitHub (full tree, not patch-only)
Upload/merge the FULL contents of:
`3_GITHUB_UPLOAD_STRUCTURE/yuny-suno-os-main/`
into the repository root. Do not upload only changed files — the runtime expects the complete material tree (knowledge-evolving, genre-dictionary, lyric-craft, production-engineering, prompt-patterns, suno-render-behavior, cases, tests, operator-history, vocal-palette, archive).

## Repo mirror (kept byte-identical to the install source)
- `project-sync/PROJECT_INSTRUCTIONS.txt`
- `project-sync/PROJECT_INSTRUCTIONS_CURRENT.txt`
- `project-sync/knowledge/` (20)
- `project-sync/knowledge-20/` (20)

## Runtime summary
- First 8 fields = FIRST EXECUTABLE DRAFT until self-tested; CREATE builds source audio; COVER is the final record from CREATE audio + COVER prompt + COVER lyric.
- LYRIC and COVER LYRIC are under-5000 performance/staging runways; cues are added only after the lyric survives without them.
- Internal production council + cross-field repair run before FINAL-CANDIDATE.
- Post-render feedback routes by stage: first-draft design / CREATE render / COVER render / final-candidate.

## Legacy / reference (NOT install source)
`archive/` and any historically versioned file are reference/craft material. The runtime never routes install through them; they are preserved on purpose.

## Status
Prepared only. The live Project changes only after you apply it manually. GitHub is current only when the full tree above is uploaded to repo root.
