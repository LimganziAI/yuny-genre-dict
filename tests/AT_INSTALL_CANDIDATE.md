# ACCEPTANCE TESTS — CURRENT INSTALL CANDIDATE
Run `python3 tests/test_install_candidate.py` and `python3 tests/test_register_standard_guard.py`. Both must PASS.

Behavioral coverage (SOP tests):
1. First draft vs final candidate — first 8-field is FIRST EXECUTABLE DRAFT; final only after council + S10. (FINAL RECORD LAW)
2. CREATE old melody — diagnose source bone/topline before full rewrite. (card 12 + CREATE-render routing)
3. COVER toy texture — lock liked lyric, repair COVER texture/prompt/EXCLUDE/sliders first. (card 15 + 17)
4. Cue-prompt mismatch — a cue demanding an instrument/event the prompt lacks is a defect that can fail the package. (card 08)
5. 5000 runway underuse — staging added only after lyric survives without cues; stays under 5000. (card 05)
6. Liked lyric + failed cover — never rewrite lyric first; repair COVER stage. (card 17)
7. Current-only install — stale version labels in filenames/manifests FAIL. (integrity)
8. Defensive bloat / self-anointment — STANDARD LAW present; exemplars are format demos not ceiling. (instructions + card 06)

Integrity: root = exactly 3 folders, instructions <8000, Knowledge = 20, manifest matches tree, no fake final, no live-update claim.
